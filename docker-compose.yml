services:
  app:
    build: .
    container_name: contract-system
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # 持久化向量库（Chroma），容器重建不丢检索库
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_data:
