# 合同系统认证考核评分标准（100分制）

> **核心思想一句话：企业要的不是"会调 API 的人"，而是"真正能把 AI 落地到业务里的人"。**
> 本考核以「合同管理系统」为载体，检验你能否用 AI 工程的方法（提示词工程 + RAG + 编排 + 评估闭环）解决真实业务问题（变量提取、合同分类、业财一体化）。

---

## 一、分数总览（先看这张表）

| # | 考核模块 | 分值 | 拿分关键 | 传说中的"权重王" |
|---|---|---|---|---|
| A | 提示词的两个层次 | 10 | 光会写提示词不够，要提示词+架构 | |
| B | Vibecoding 完成合同系统 | 25 | 合同变量提取是灵魂；业财一体化是天花板 | ★核心业务 |
| C | 框架选型（Odoo vs Flask） | 5 | 用模型驱动的 Odoo 自定义模块更高分 | |
| D | AI 底座：Embedding / 向量库 / RAG | **30** | **全场最大权重**，见下方红字 | ★★★必争之地 |
| E | PDF OCR 场景选型 | 10 | 不是无脑 OCR，按情况选型 | |
| F | Transformer 与向量库原理 | 10 | 白板讲得清才算懂 | |
| G | 工程化交付 | 10 | GitHub + 全流程 + 演示视频 | |
| | **合计** | **100** | **≥60 分通过认证** | |

> ⚠️ **D 模块单独强调**：项目考察最大权重的就是 —— 是否用到 **Embedding + 向量库**，以及 **把 RAG 当监督学习用**（在向量库里预置几个类型正确的合同样本做参照比对）。这一条直接拉开 20 分差距。

---

## 二、六大考核细则

### 考核 A：提示词的两个层次（10 分）

企业视角：只会写一句"请帮我提取合同金额"，提示词不稳定、换份合同就崩 —— 这是入门水平。

| 得分 | 层次 | 判定标准 |
|---|---|---|
| 1–4 分 | **层次①：裸提示词** | 写了单条提示词能提出部分字段，但无格式约束、无异常处理，换合同就不稳定 |
| 5–8 分 | **层次②：提示词 + 架构** | 全部满足：<br>① JSON Schema 结构化输出强制约束<br>② Few-shot 示例内嵌<br>③ 字段字典/枚举约束（如合同类型只能取既定枚举）<br>④ 解析失败自动重试兜底逻辑 |
| 9–10 分 | **层次③：提示词工程体系** | 分类提示词与提取提示词分层拆解；版本化管理；有一组"金标准合同"做回归测试，改提示词能看到准确率变化 |

**减分项**：提示词写死单一模板，无法适配不同合同版式（-2）。

---

### 考核 B：Vibecoding 做出整个合同系统（25 分）

> **用 AI 协作写出"一个函数"和用 AI 协作写出"一个系统"是两个概念。**
> 判定核心：你是不是用 vibecoding 从需求 → 设计 → 模块划分 → 实现 → 测试走完了整个系统，而不是只让 AI 补了几个局部函数。

#### B1 合同变量提取（10 分）★ 课程主考核点，AI 价值最大的地方

上传 PDF → 解析 → LLM 提取 → 结构化入库，打通算基本盘：

| 小项 | 分值 | 给分标准 |
|---|---|---|
| 功能链路跑通 | 4 | 上传合同 → 自动提取字段 → 入库 → 页面回显，全链路可演示 |
| **提取准确率**（教师提供 10 份标准合同盲测） | 4 | **≥95% 得 4 分；90–95% 得 3 分；80–90% 得 2 分；70–80% 得 1 分；<70% 得 0 分** |
| 字段完备性 | 2 | 至少覆盖：合同名称、编号、甲方、乙方、合同金额（含大写校验）、签订日期、履行期限、付款方式、违约责任、争议解决 |

> 准确率算法 = 正确提取字段数 ÷ 应提取字段总数（逐字段核对，日期/金额须语义等价而非字符串相同）。

#### B2 合同分类（4 分）

能做到"这是采购合同、那是销售合同"的自动分类：

| 小项 | 分值 | 给分标准 |
|---|---|---|
| 分类能力 | 2 | 支持至少 4 类（采购/销售/服务/租赁等），LLM+规则双通道，输出置信度 |
| 分类准确率 | 2 | 教师 10 份样本盲测 ≥90% 得 2 分，否则按比例给分 |

#### B3 合同信息管理（5 分）—— 「合同管理系统」商用蓝本

商用系统长什么样？以下对标真实产品，每实现一块得对应分（实现含义即可得一半分，跑通交互得满分）：

| 子功能 | 分值 | 系统里的对应物（参考对象） |
|---|---|---|
| 合同列表/详情/状态流转（草拟→审批→用印→归档→作废） | 1.5 |  `contract` 域：主体台账 + 事件驱动状态机 |
| 模板 + 条款库 + 要素拼装起草 | 1.5 |  `template` 域：TemplateContract + TemplateClause + TemplateElement 三件套 |
| 配置域：相对方、签约主体、编号规则 | 1 |  `conf` 域 |
| 台账/看板/导出报表 | 1 |  `report` 域 |

#### B4 商用级完整度（3 分）

系统不是 demo 玩具，而是"可以拿来商用的合同系统"——登录鉴权、异常处理、界面可用、演示时全程不出 bug、有自己的技术方案文档。评审现场随便点点点不崩 = 满分。

#### B5 业财一体化（3 分）★★ 最高分的分水岭

**理解"合同是业财一体化的依据"**：合同里藏着收付款节点 → 驱动应收应付 → 收付款核销 → 经营台账。

- 有口头/文档层的正确理解 + 方案设计：1.5 分
- 系统真的实现了雏形（如：从合同提取付款节点 → 自动生成收付款计划 → 台账联动）：3 分

---

### 考核 C：框架选型 —— Odoo 是高级解（5 分）

> Odoo 是开源的企业级**模型驱动**框架：定义 Python Model 即获得 ORM + 视图 + 权限 + 菜单 + 工作流的整套企业级底座，比手撸 Flask 更接近真实企业产品的生产效率。

| 得分 | 标准 |
|---|---|
| 0–2 分 | 仅口头比较过两者，未体现在项目中 |
| 3 分 | 用 Flask/FastAPI 实现了完整功能（承认你的工程量，但架构分到此为止） |
| 5 分 | 用 **Odoo 自定义模块**承载合同业务：自建 model（合同/相对方/签约主体）+ view + 权限组，**遵守铁律：不改 Odoo 源码、不动业务原生能力，只写 addons** |

附加理念分（计入本模块的表述质量）：能说出三个"解耦"—— 业务解耦（AI 服务与 Odoo 业务解耦）、技术解耦（LLM 可替换）、编排解耦（Agent 编排层独立成层）。

---

### 考核 D：AI 底座 —— Embedding / 向量库 / RAG / 编排 / 评估（30 分）★★★ 最大权重

#### D1 Embedding + 向量库接入（5 分）

| 小项 | 分值 |
|---|---|
| 接入任一向量库（Chroma / Milvus / pgvector 均可），合同文本切块、向量化、入库、检索可视化 | 3 |
| 切块策略有讲究（按条款/语义切而非无脑 500 字硬切），并说明理由 | 2 |

#### D2 把 RAG 当监督学习用（10 分）★ 最核心的拉分点

思想：**不需要训练模型**。在向量库里预先放入几个"类型正确、字段规范"的标准合同样本（如采购合同范例 2 条、销售合同范例 2 条），新合同进来先做向量相似检索，把最像的范例捞出来当 few-shot 参照，再喂给 LLM 提取 —— 提取准确率显著提升。

| 小项 | 分值 |
|---|---|
| 向量库中有正确类型的标注范例集（≥4 类合同各若干条） | 3 |
| 检索到的范例确实进入 Prompt 作为参照（few-shot 注入，日志可见） | 3 |
| **拿出对比实验数据：无 few-shot vs 有 few-shot 的准确率变化**（如 82% → 93%） | 4 |

#### D3 LangChain vs LangGraph 选型（5 分）

答对场景才给分，用错场景不给分：

| 场景 | 正确选择 | 为什么 |
|---|---|---|
| 线性流水线：解析 → 提取 → 入库 | **LangChain**（Chain/Runnable 串行管道足矣） | 无状态、无分支、一遍跑完 |
| 审查 Agent：审查 → 发现风险 → 回头补材料 → 重新审 → 循环/分支/人工介入 | **LangGraph**（图编排，自带状态机和 checkpoint） | 有状态、有回路、需要中断恢复 |

- 选型判断题现场问 2 题，答对得 2 分；
- 项目中两类场景各自选用了正确的框架并能解释原因，得 3 分。

*（LlamaIndex、Deep Agents 见第六节"锦上添花"，不占此分。）*

#### D4 RAGAS 评估框架（5 分）

会评估 RAG，才算真会用 RAG。用 RAGAS 或等价自建评估脚本均可：

| 小项 | 分值 |
|---|---|
| 至少跑通 2 个指标：Faithfulness（忠实度）、Answer Relevancy（答案相关性） | 2 |
| Context Precision / Context Recall（检索层评估）也跑了 | 2 |
| 输出一份评估报告，并针对低分项做过一轮改进 | 1 |

> 这一条对应业务口径：**60 分及格线的本质 = 你的提示词评估、Embedding 评估、RAG 评估三件套做出来了。**

#### D5 Deep Agents 多智能体（5 分）

- 了解层级（说出"规划 + 工具调用 + 反思迭代"的多 Agent 机制）：2 分
- 项目中有实际 multi-agent 任务：如"分类 Agent → 提取 Agent → 校验 Agent → 审查 Agent"接力，出错能自我修正重来：5 分

---

### 考核 E：PDF OCR 场景选型（10 分）

不同情况的 PDF 要不同打法，无脑上一个引擎说明你不懂工程：

| 场景 | 正确选型 | 分值 |
|---|---|---|
| 文字版 PDF（能复制文字的那种） | 直接文本抽取（pdfplumber 等），**根本不需要 OCR** —— 能意识到这点 +2 | 2 |
| 扫描件/图片型 PDF | PaddleOCR / DeepSeek OCR 类视觉模型，说清本地部署条件 | 3 |
| 复杂表格、盖骑缝章、歪斜脏污 | 表格识别专用管线（PP-Structure 类）+ 图像预处理（纠偏去噪） | 2 |
| 用文字版 vs 扫描版的判定逻辑做分流（程序自动判断走哪条管线） | 证明你会工程化选型而不靠人肉 | 1 |
| 同一批样本上两套方案的准确率对比数据 | 2 |

---

### 考核 F：Transformer 与向量库原理（10 分）

不看背诵，看你能不能"结合本项目讲明白"：

| 小项 | 分值 | 标准 |
|---|---|---|
| 文本如何变成向量 | 3 | Tokenization → Embedding，能把一段合同变成 1024/1536 维向量，维度里存的是"语义坐标"这件事讲清楚 |
| Attention 一句话直觉 + 余弦相似度 | 3 | QKV 各干什么；为什么"采购合同"和"购销协议"向量近、"采购合同"和"员工手册"远 |
| 全链路白板推演 | 4 | 合同切块 → 向量化 → 存向量库（索引如 HNSW）→ 相似检索 → 结果回填 Prompt，一口气画出来讲通 —— 这是"深刻理解本项目"的直接证据 |

---

### 考核 G：工程化交付（10 分）

| 小项 | 分值 | 标准 |
|---|---|---|
| GitHub 仓库 | 3 | 提交记录连续可见开发过程；README 说清楚是什么/怎么跑/架构图 |
| 开发→测试→上线全流程意识 | 4 | 有需求/接口文档、测试用例（含准确率测试脚本）、Docker 化或一键启动脚本 —— 对应面试常问"你们真实项目怎么走完上线流程" |
| 演示视频 | 3 | ≤10 分钟：完整演示核心链路 + 讲清架构与准确率数据（录屏+口播） |

---

## 三、分数档次（学生自查标尺）

| 档次 | 分数 | 你处于什么水平（对应业务口径） |
|---|---|---|
| 未通过 | <60 | 链路没跑通 / 提取准确率 <70% / 评估工程缺失 |
| **合格** | 60–69 | **对应业务 60 分线**：三大评估工程（提示词/Embedding/RAG）都做了；变量提取准确率 ≥80%；链路可演示。达到就业底线 |
| 良好 | 70–84 | **对应业务 80 分线**：变量提取准确率 ≥90%；LangChain/LangGraph 使用得当；RAGAS 出报告；OCR 按场景分流；系统完整可交互 |
| 优秀 | 85–94 | RAG few-shot 监督学习有**对比数据**（如 82%→93%）；Odoo 自定义模块化；系统达商用完整度（高分线） |
| 满分 | 95–100 | **对应业务 100 分线**：业财一体化实现 + 深刻理解（最高分标志）；Transformer 全链路白板通关；整套方案可拿去投标的成熟度 |

---

## 四、内功 vs 锦上添花（面试防身表）

### 必修内功（面试官会往死里问，必须懂透）

1. **字段提取准确率方法论**：怎么测、怎么优化（few-shot 注入 / 字段约束 / 分层提示词 / 重试兜底）—— 课程灵魂
2. **Embedding 与余弦相似度原理**（考核 F 就是给你压这个内功）
3. **RAG 全链路与三种评估**
4. **LangChain 和 LangGraph 的边界**
5. **业务语言**：说得清"业财一体化"里合同扮演的角色 —— 让业务方觉得你可信的那部分

### 了解即可，别纠结（锦上添花）

> 这些内容课堂不展开。但"了解即可"不等于"一头雾水"——下面每条都给你三件套：**一句话人话解释 / 什么时候才需要学深 / 自学提示词**。花 10 分钟看完心里有数就行，千万别陷进去。

#### ① LlamaIndex

| 问题 | 人话答案 |
|---|---|
| 它是什么 | 偏"数据接入与索引层"的框架：擅长把各种数据源（PDF、网页、数据库）接进来建索引、做检索 |
| 和 LangChain 啥关系 | LangChain 偏"流程编排"，LlamaIndex 偏"数据索引"。本项目切块+向量化用 LangChain 就够了 |
| 何时需要学深 | 将来对接海量异构文档源、需要高级索引策略时再回头学，半天入门 |

```text
【自学提示词】你是 5 年经验的 RAG 工程师。我已用 Python 写好合同系统，技术栈 LangChain + Chroma 向量库。
请用初中生能听懂的话，对比 LlamaIndex 与 LangChain 在"文档接入与索引"上的分工差异；
给一个 LlamaIndex 读取合同 PDF 并做相似检索的最小示例代码；
最后明确告诉我：什么信号出现时，我才值得迁移到 LlamaIndex？
```

#### ② 微调 Fine-tuning 与 LoRA

| 问题 | 人话答案 |
|---|---|
| 它是什么 | 用自己的数据继续训练模型参数，把知识和套路"焊进"模型里；LoRA 是低成本的微调技术 |
| 本项目为啥不用 | 准确率不够时，往向量库里加几条正确范例（few-shot 检索）立刻见效；微调则费卡费钱、改一次重训一次，还不好排查问题 |
| 何时需要学深 | 当正确范例多到几百上千条、Prompt 根本装不下，或行业术语太特殊时，才轮到微调出场 |

```text
【自学提示词】你是大模型训练工程师。我有几百份已标注成 JSON 字段的合同，目前用 RAG few-shot 方案提取准确率 93%。
请帮我分析：出现什么情况才应该从 few-shot 升级到微调？从数据量、成本、迭代速度三个维度对比；
若要微调，给出用 LoRA 训练合同提取模型的最小步骤清单（数据格式→训练→评估），不要空话。
```

#### ③ K8s、高并发、向量库性能调优

| 问题 | 人话答案 |
|---|---|
| 它是什么 | 系统上线后的大规模运维与性能课题：容器编排、百万级向量的索引选型与召回率调优 |
| 为啥不用纠结 | 本项目量级是十份百份合同，单机 Chroma 绰绰有余；调优是"百万数据的烦恼"，没那个量谈了也白谈 |
| 何时需要学深 | 入职后真遇到"库大了查询慢"再学完全来得及——原理（HNSW）你在考核 F 已经懂了 |

```text
【自学提示词】你是向量检索架构师。我的合同向量库未来约 100 万条，现在用 Chroma + HNSW。
请通俗对比 HNSW 与 IVF 在速度/内存/召回率上的取舍，做成一张决策表：数据量到了哪一档该换什么方案；
再教我用哪几个指标监控"什么时候该调优了"。
```

#### ④ 各种花样 Agent 框架的全家桶

| 问题 | 人话答案 |
|---|---|
| 它是什么 | AutoGen、CrewAI 等层出不穷的多智能体框架 |
| 为啥不用纠结 | 所有 Agent 框架底层都是三件事：**规划 → 工具调用 → 反思循环**（Deep Agents 这套，考核 D5 已覆盖）。框架只是壳，原理不变 |
| 正确的学习姿势 | 先把 LangGraph 的图编排吃透，其他框架自然触类旁通 |

```text
【自学提示词】你是 Agent 架构顾问。我在做"分类→提取→校验→审查"多Agent合同流水线，已在用 LangGraph。
请按 编排模型/状态管理/学习成本 三个维度，把 AutoGen、CrewAI、LangGraph 列成对比表，
并直接下结论：以我的需求该用哪个、另外两个是什么时候的选项？
```

#### ⑤ 自学提示词的万能公式（写给自己也写给 AI）

这四段提示词共同的结构，就是一套可以复用的提问模板：

```text
你是【某领域的专家】            ← 给 AI 定角色，回答的专业度立刻上升
我的现状是【技术栈+已完成的事】  ← 背景信息越具体，答案越贴身不带套话
请解决【具体问题】              ← 问题要窄，别问"讲讲RAG"这种大问题
输出形式：【对比表/最小代码/清单】，语言【中学生能懂】  ← 不约束格式，就会得到散文
```

> 一句话：**把 80% 时间砸在 B1/D2 两条线上，这两条线就是 35 分和你的饭碗；上面这些等到用时再"喂着提示词学"。**

---

## 五、答辩前的最后自查清单

- [ ] 10 份教师盲测合同跑过了吗？准确率数字是多少？
- [ ] 向量库里有没有类型正确的范例？有没有 few-shot 对比数据？
- [ ] LangChain 和 LangGraph 分别用在哪个环节？能一句话说清为什么？
- [ ] RAGAS 四个指标里至少两个有数吗？
- [ ] 文字版 PDF 和扫描版 PDF 走的是不同管线吗？
- [ ] 能脱稿画出 合同→切块→向量→检索→Prompt 的全链路图吗？
- [ ] 系统陌生点开 10 分钟会不会崩？
- [ ] 能不能讲清楚：我的系统怎么把合同变成财务的收付款依据？

---

*评价规则补充：同一问题按最高档计分，不重复计分；组队项目按成员分工细分举证（答辩现场随机指定成员回答模块内部问题，答不上则该成员该项折半）。*
